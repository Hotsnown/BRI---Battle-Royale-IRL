// it should begin if and only if launch event has been launched
// it should know who are the players
// it should access the current position of the players
// it should return :
// - the current center of players

import firebaseApp from '../firebase/Firebase';

export function init() {
    
}